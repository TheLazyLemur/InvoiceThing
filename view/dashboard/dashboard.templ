package dashboard 

import "invoicething/view/layout"

templ Show (isLoggedIn bool, route string) {
	@layout.Base(isLoggedIn, route) {
		<div class="flex h-[93%]">
			<div class="w-[10%] h-full bg-red-300"></div>
			<div class="w-[90%] h-full bg-blue-300"></div>
		</div>
	}
}
